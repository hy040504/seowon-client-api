import { createHopeBasketClient } from './src/index.js'; async function run() { const basket = createHopeBasketClient({ cookieFilePath: '.seowon-hope-basket.cookies.json' }); await basket.login({ stuno: process.env.SEOWON_ID || '', password: process.env.SEOWON_PASSWORD || '' }); const res1 = await basket.searchSubjects({ keyword: '코딩' }); console.log('Without asignDeprtCd:', res1.length); const res2 = await basket.searchSubjects({ keyword: '코딩', serchDiv: '1', cltrDomnCd: '01' }); console.log('With serchDiv 1:', res2.length); } run();
