# Session 115

## Summary

- Time: 2026-07-31T18:57:56.12843+09:00 -> 2026-07-31T18:57:56.1675669+09:00
- Method: GET
- URL: https://sugangh.seowon.ac.kr/nx/lib/libGrid.xjs.js
- Status: HTTP/1.1 200 OK
- Response bytes: 15620
- Content-Type: application/javascript

## Request Line

```
GET https://sugangh.seowon.ac.kr/nx/lib/libGrid.xjs.js HTTP/1.1
```

## Request Headers

```
Host: sugangh.seowon.ac.kr
Connection: keep-alive
sec-ch-ua-platform: "Windows"
cache-control: no-cache
User-Agent: Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/150.0.0.0 Safari/537.36
sec-ch-ua: "Not;A=Brand";v="8", "Chromium";v="150", "Google Chrome";v="150"
sec-ch-ua-mobile: ?0
Accept: */*
Sec-Fetch-Site: same-origin
Sec-Fetch-Mode: cors
Sec-Fetch-Dest: empty
Referer: https://sugangh.seowon.ac.kr/nx/
Accept-Encoding: gzip, deflate, br, zstd
Accept-Language: ko,ko-KR;q=0.9,en-US;q=0.8,en;q=0.7
Cookie: SESSIONID=50d37990-8045-4a54-8017-2439c49f7d50
```

## Request Body Preview

```
(empty)
```

## Response Status

```
HTTP/1.1 200 OK
```

## Response Headers

```
Date: Fri, 31 Jul 2026 09:57:56 GMT
Last-Modified: Fri, 31 Jul 2026 09:40:48 GMT
ETag: "3bf8-657e4fbc2a7be"
Accept-Ranges: bytes
Content-Length: 15352
Keep-Alive: timeout=5, max=98
Connection: Keep-Alive
Content-Type: application/javascript
```

## Response Body Preview

```
//XJS=libGrid.xjs
(function()
{
    return function(path)
    {
        var obj;

        // User Script
        this.registerScript(path, function() {

        /*******************************************************************************************************************************************************************************************
         * Function Name: gfn_gridSort Description : 그리드의 Sort를 처리한다.
         * Arguments : object Grid, GridClickEventInfo e
         * Return : None
         ******************************************************************************************************************************************************************************************/
        this.gfn_gridSort = function(obj, e) {
            // 컬럼의 정렬방식을 'head'의 text에 "↑,↓"여부로 판단.
            // 이미지로 대체 가능.
            // sort 방법 asc -> desc -> 해제 -> asc -> desc
            var strType = obj.getCellProperty("head", e.cell, "displaytype");
            if (strType == "checkbox") {
                return;
            }

            var bindDs = obj.getBindDataset();
        	if (!bindDs) {
                return;
            }


            if (bindDs.getRowCount() == 0) {
                return false;
            }

            var BodyColId = (obj.getCellProperty("body", e.col, "text")).toString().split(":");

            if (BodyColId.length != 2)
                return;

            var sortStatus = "";
            var sSaveHeadText = "";

            // bindDs.set_enableevent(false);
            for (var i = 0; i < obj.getCellCount("head"); i++) {
                var strHeadText = obj.getCellText(-1, i);
        		if (!this.utils.isValid(strHeadText) || obj.getCellText(-1, i) == "undefined") {
                    continue;
                }

                if (i == e.cell) {
                    if (strHeadText.substr(strHeadText.length - 1) == "▲") {
                        obj.setCellProperty("head", i, "text", strHeadText.substr(0, strHeadText.length - 1) + "▼");
                        bindDs.set_keystring(("S:-" + BodyColId[1]));
                        sortStatus = 2;
                        sSaveHeadText = strHeadText.substr(0, strHeadText.length - 1);
                    } else if (strHeadText.substr(strHeadText.length - 1) == "▼") {
                        this.gfn_clearSortMark(obj, null, "COM");
                        bindDs.set_keystring("");
                        obj.user_orgSort = ""; // 공통에서 sort한 정보 저장
                        return;
                    } else {
                        obj.setCellProperty("head", i, "text", strHeadText + "▲");
                        bindDs.set_keystring(("S:+" + BodyColId[1]));
                        sortStatus = 1;
                        sSaveHeadText = strHeadText;
                    }
        			obj._commonUser_sortKeyString = (sortStatus == 1 ? "S:+" : "S:-") + BodyColId[1];

                } else {
                    // 정렬표시 삭제
                    if (strHeadText.substr(strHeadText.length - 1) == "▲" || strHeadText.substr(strHeadText.length - 1) == "▼") {
                        obj.setCellProperty("head", i, "text", strHeadText.substr(0, strHeadText.length - 1));
                    }
                }
            }

            obj.user_orgSort = bindDs.keystring; // 공통에서 sort한 정보 저장

            // 대상 grid 에 정렬정보를 가지는 사용자 속성 확인/추가
        	obj._commonUser_sortInfos = {};

            // 정렬대상컬럼 (순서중요)
        	obj._commonUser_sortItems = [];

            var refCell;
            var format = obj._curFormat;
            refCell = format._headcells[e.cell];

            var sortInfos = obj._commonUser_sortInfos;
        	var sortItems = obj._commonUser_sortItems;
        	var sortInfo = obj._commonUser_sortInfos[BodyColId[1]];

            if (!this.utils.isValid(sortInfo)) {
                // executeSort에서 정렬 표시를 위해 cell index 가 필요한데
                // cell

... [truncated 10636 chars]
```
