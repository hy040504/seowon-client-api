# Session 020

## Summary

- Time: 2026-07-31T18:57:37.0162573+09:00 -> 2026-07-31T18:57:37.0385541+09:00
- Method: GET
- URL: https://sugangh.seowon.ac.kr/nx/lib/libForm.xjs.js
- Status: HTTP/1.1 200 OK
- Response bytes: 19760
- Content-Type: application/javascript

## Request Line

```
GET https://sugangh.seowon.ac.kr/nx/lib/libForm.xjs.js HTTP/1.1
```

## Request Headers

```
Host: sugangh.seowon.ac.kr
Connection: keep-alive
sec-ch-ua-platform: "Windows"
cache-control: no-cache
User-Agent: Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/150.0.0.0 Safari/537.36
sec-ch-ua: "Not;A=Brand";v="8", "Chromium";v="150", "Google Chrome";v="150"
sec-ch-ua-mobile: ?0
Accept: */*
Sec-Fetch-Site: same-origin
Sec-Fetch-Mode: cors
Sec-Fetch-Dest: empty
Referer: https://sugangh.seowon.ac.kr/nx/
Accept-Encoding: gzip, deflate, br, zstd
Accept-Language: ko,ko-KR;q=0.9,en-US;q=0.8,en;q=0.7
```

## Request Body Preview

```
(empty)
```

## Response Status

```
HTTP/1.1 200 OK
```

## Response Headers

```
Date: Fri, 31 Jul 2026 09:57:37 GMT
Last-Modified: Fri, 31 Jul 2026 09:40:48 GMT
ETag: "4c24-657e4fbc2a7be"
Accept-Ranges: bytes
Content-Length: 19492
Keep-Alive: timeout=5, max=98
Connection: Keep-Alive
Content-Type: application/javascript
```

## Response Body Preview

```
//XJS=libForm.xjs
(function()
{
    return function(path)
    {
        var obj;

        // User Script
        this.registerScript(path, function() {
        this.lvForm = this;
        this._rtnModal=null;

        /********************************************************************************
         * Function Name    : gfn_setTitle                                              *
         * Desc             : 다국어 설정
         * Parameter        : obj - 대상 obj
         * Return           : 없음
         ********************************************************************************/
        this.gfn_setLanguage = function (arrObj)
        {
            var application = nexacro.getApplication();
        	for (var i=0; i<arrObj.length; i++) {
                var obj = arrObj[i];
        		if (obj instanceof Grid) {
        			this.gfn_setGridText(obj);
        		} else if (obj instanceof Tab) {
        			this.gfn_setTabText(obj);
        		} else if (obj instanceof Static) {
        			this.gfn_setStaticText(obj);
        		} else if (obj instanceof Button) {
        			if (obj.cssclass == "btn_WFSA_Search") {
        				if (application.locale == "en") {
        				   obj.set_width(82);
        				   obj.set_right(9);
        				   obj.set_cssclass("btn_WFSA_SearchEN");
        				}
        			} else {
        				this.gfn_setButtonText(obj);
        			}
        		} else if(obj instanceof CheckBox) {
                    this.gfn_setCheckBox(obj);
        		} else if (obj instanceof Div) {
        			if (obj.cssclass == "div_WF_Important" || (obj.text && obj.text !== "")) {
        				this.gfn_setImportantDivText(obj);
        			}
        		}
            }
        }
        /**************************************************************************************************
         * Function Name	: gfn_setGridText                                                              *
         * Desc				: 그리드 헤더 텍스트 변경 처리    										   *
         * Parameter		: objGrid - Grid							  								       *
         * Return 			: 사용자가 선택한 다국어 라벨 값									       *
         **************************************************************************************************/
        this.gfn_setGridText = function (objGrid)
        {
        	for (var i = 0; i < objGrid.getCellCount("head"); i++) {

        		if (objGrid.getCellProperty("head", i, "displaytype") == "checkbox")
        		    continue;

        		if(objGrid.getCellText(-1, i)=="undefined" || objGrid.getCellText(-1, i)=="_chk")
        			continue;

        		var strHeadText = objGrid.getCellText(-1, i);
        		var strTemp = this.gfn_getLabel(strHeadText);
        		objGrid.setCellProperty( "head", i, "text", strTemp);
        		objGrid.setCellProperty( "head", i, "tooltiptext", strTemp);
        	}

        	for (var i = 0; i < objGrid.getCellCount('Summ'); i++) {

        		if(objGrid.getCellText(-1, i)=="undefined")
        			continue;

        		var strHeadText = objGrid.getCellText(-2, i);
        		var strTemp = this.gfn_getLabel(strHeadText);

        		objGrid.setCellProperty( "Summ", i, "text", strTemp);
        	}
        }

        /********************************************************************************
         * Function Name	: gfn_setTabText											*
         * Desc				: Tab 텍스트 변경 처리    										   *
         * Parameter		: obj(Tab)													*
         * Return 			:															*
         ********************************************************************************/
        this.gfn_setTabText = function (objTab)
        {
            if (!objTab.preload)
            {
                objTab.addEventHandler("onchanged", this.gfn_tabChanged, this);
            }

        	var objPages = objTab.tabpages;

        	for(var i = 0 ; i < objPages.length ; i++){
        		var sTitle = objPages[i].text;
        		objPages[i].set_text(thi

... [truncated 14780 chars]
```
