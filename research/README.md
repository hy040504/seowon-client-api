# research

로컬 역공학·패킷 분석용 참고 자료 디렉터리입니다. 패키지 런타임 코드가 아닙니다.

| 경로 | 내용 |
| :--- | :--- |
| `saz/` | Fiddler SAZ 원본 및 확장 분석 (`*.saz`·`_expanded` 등) |

- 계정·세션·개인정보가 포함될 수 있으므로 Git에 올리지 마세요 (`research/saz/`는 `.gitignore` 대상).
- 테스트 픽스처는 `research/saz/` → 레거시 `SAZ/` → `files/` 순으로 탐색합니다.
